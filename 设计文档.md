# 语音绘图工具 · 设计文档

> **题目要求**：用户不使用鼠标或键盘操作画布，仅通过语音指令完成绘图创作。需综合考虑指令理解的准确性与容错性、语音到绘图操作的响应延迟，以及复杂指令的拆解与执行能力。

---

## 1. 问题定义

绘图是连续、精确的空间操作；语音是模糊、离散的自然语言。本项目的核心工程问题：

1. 如何把口语（含 ASR 识别错误）稳定映射为可执行的绘图指令？
2. 如何在可接受的延迟内完成「说完 → 出图」？
3. 如何把复合语句拆解为可顺序执行的操作序列？

**产品范式**：不做自由手绘（语音无法稳定表达连续轨迹坐标）。采用 **AI 文生图 + 对话式改图 + 分阶段作画动画** 作为创作主路径，辅以 **参数化矢量图形** 处理简单几何与编辑操作。

---

## 2. 交付形态与技术选型

| 层次 | 选型 | 理由 |
|---|---|---|
| 应用形态 | Web 应用（`run_web.bat`） | 现代 UI、Canvas 动画、易演示 |
| 前端 | HTML + Canvas + ES Modules | 分阶段作画、实时字幕 |
| 后端 | FastAPI + uvicorn | 统一代理 LLM / 万相 / ASR |
| ASR（主） | 浏览器 Web Speech API | 低延迟、边说边出字 |
| ASR（备） | 阿里云 Paraformer + Whisper | 浏览器不可用时降级 |
| NLU | DeepSeek LLM | 理解任意自然语言，拆解复合指令 |
| 文生图 | 通义万相 `wan2.6-t2i` | 高质量插画/场景生成 |
| 改图 | 通义万相 `wan2.6-image` | 在原图基础上按指令编辑 |
| 分阶段作画 | `paint_stages.py`（OpenCV） | 从成图派生底稿/色块，前端逐层动画 |
| TTS | 浏览器 SpeechSynthesis | 语音反馈闭环 |

### 纯语音交互说明

- **画布操作**：全部通过语音指令驱动，用户无需用鼠标/键盘在画布上绘制或选中。
- **必要例外**：首次需点击「开始聆听」以获取浏览器麦克风权限。
- **导出**：可说「保存图片」，或点右上角「导出」按钮（仅下载，不算画布绘制操作）。
- 侧边栏文字输入为**调试/无障碍备选**。

---

## 3. 系统架构

```
麦克风
      ↓ HybridASR（浏览器连续识别，优先）
      │   └─ 失败时 → 录音 + VAD → POST /api/transcribe（云端/Whisper）
识别文本（含临时字幕）
      ↓ POST /api/chat（画布上下文 + 对话历史）
DeepSeek LLM → 结构化 commands JSON
      ↓
前端 Executor 分阶段串行执行
      ├─ generate_image → POST /api/generate-image → 万相成图
      │     └─ paint_stages 生成底稿/色块 → 前端 paintStages 动画
      ├─ edit_image     → POST /api/edit-image     → 万相改图 → 重新揭示
      ├─ export         → canvas.toDataURL → 浏览器下载 PNG
      └─ move / resize / delete / clear / undo … → Canvas 引擎
      ↓
TTS 播报（播报期间暂停收音，防回声）+ 对话日志
```

---

## 4. 三大要求的实现方案

### 4.1 指令理解准确性与容错性

| 机制 | 实现 |
|---|---|
| LLM 主脑 | 理解任意口语，输出结构化 `commands` 数组 |
| 画布上下文注入 | 图形列表、AI 图片描述传给 LLM，支持「移动这张图」「给猫戴帽子」 |
| 对话历史 | 最近多轮对话传入 LLM，支持多轮修正 |
| 指令规范化 | `_normalize_commands`：图片操作用 `target:image`，禁止误写 `color` |
| 目标解析兜底 | 画布仅有一张图时，`move/delete` 自动落到该图 |
| 空结果重试 | LLM 返回空 `commands` 时自动二次请求 |
| 网络容错 | API 连接中断自动重试 + `Connection: close` |
| 澄清反馈 | 无法理解时 TTS + 日志提示，不静默失败 |

### 4.2 语音到绘图响应延迟

| 环节 | 典型延迟 | 优化手段 |
|---|---|---|
| 浏览器 ASR | 实时 ~0.5s | Web Speech API 连续 + 临时结果 |
| 云端 ASR（备用） | 2–4s | 直接上传 webm，缩短 VAD 静音等待 |
| LLM 理解 | 3–15s | 超时重试；精简上下文 |
| 文生图 | 10–30s | Loading 提示 + 分阶段动画降低等待焦虑 |
| 改图 | 15–40s | 同上 |
| 矢量/编辑 | <100ms | 本地 Canvas 即时渲染 |

**瓶颈**：AI 生图/改图受云端 API 限制。策略：简单几何即时响应，复杂创作走 AI 并给出明确等待提示。

### 4.3 复杂指令拆解与执行

LLM 输出 `commands` 数组，前端按类型分阶段执行：

| 阶段 | 指令类型 | 示例 |
|---|---|---|
| 1 | 非绘制类 | 清空、移动、删除、导出 |
| 2 | 矢量笔画 | `stroke` 逐笔动画 |
| 3 | 文生图 | `generate_image` → 分阶段作画 |
| 4 | 改图 | `edit_image` → 替换原图 |

**示例**：「先清空画布，然后画一只橘猫」

```json
[
  {"action":"clear"},
  {"action":"generate_image","prompt":"一只可爱的橘色小猫，纯白背景…","x":480,"y":270,"w":952,"h":532}
]
```

---

## 5. 指令能力：计划 vs 实现

| 能力 | 示例 | 计划 | 实现 | 说明 |
|---|---|:---:|:---:|---|
| **AI 文生图** | 画一只可爱的小猫 | ✅ | ✅ | 通义万相，默认创作方式 |
| **分阶段作画** | 底稿→色块→成品 | ✅ | ✅ | `paint_stages.py` + 前端动画 |
| **对话式改图** | 给猫戴顶帽子 | ✅ | ✅ | `wan2.6-image` |
| **导出画布** | 保存图片 / 导出 | ✅ | ✅ | 语音 + 按钮，下载 PNG |
| 图片铺满画布 | 默认近满幅 | ✅ | ✅ | 默认 w/h 近画布尺寸 |
| 绘制基本图形 | 画一个红色的圆 | ✅ | ✅ | |
| 矢量笔画 | 画一条线 | ✅ | ✅ | 简单线条场景 |
| 移动/缩放/删除图片 | 移到右边 / 放大 | ✅ | ✅ | `target:image` |
| 删除/清空 | 清空画布 | ✅ | ✅ | 清空直接执行，无需确认 |
| 撤销/重做 | 撤销 | ✅ | ✅ | |
| 复合指令 | …然后… | ✅ | ✅ | LLM 拆解 |
| 多轮对话改图 | 先画猫再戴帽子 | ✅ | ✅ | |
| 浏览器快速 ASR | 边说边识别 | ✅ | ✅ | HybridASR |
| 云端 ASR 兜底 | 浏览器不可用 | ✅ | ✅ | |
| TTS + 防回声 | 播报不触发循环 | ✅ | ✅ | 播报期间暂停收音 |
| 语音帮助 | 说「帮助」 | ✅ | ✅ | |

---

## 6. 未完成 / 受限部分及原因

| 项目 | 状态 | 原因 |
|---|---|---|
| 完全零键鼠（含启动） | ⚠️ 受限 | 浏览器麦克风权限需一次点击；导出按钮为辅助 |
| 唤醒词常驻监听 | ❌ 已放弃 | 浏览器限制多、体验不稳定，已移除 |
| 自由手绘笔迹 | ❌ 未实现 | 语音无法稳定表达连续坐标 |
| 多图智能排版 | ⚠️ 部分 | 支持多张图，未做自动防重叠 |
| 局部蒙版精修 | ❌ 未实现 | 改图为全图指令编辑 |
| 离线 AI 创作 | ❌ 未实现 | 依赖云端 API |

---

## 7. 模块说明

| 文件 / 目录 | 职责 |
|---|---|
| `run_web.bat` | 启动服务、释放 8765 端口、打开浏览器 |
| `voice_paint/web_server.py` | FastAPI：ASR / LLM / 文生图 / 改图 |
| `voice_paint/paint_stages.py` | 从成图生成底稿、色块素材 |
| `voice_paint/image_gen.py` | 万相文生图 + 改图，含网络重试 |
| `voice_paint/nlu/llm.py` | DeepSeek 主脑 |
| `web/js/speech.js` | HybridASR、TTS、防回声 |
| `web/js/engine.js` | Canvas、分阶段动画、导出 |
| `web/js/app.js` | 主流程、指令调度 |
| `web/js/executor.js` | 指令队列与反馈 |

---

## 8. 测试建议

1. **文生图**：「画一只橘猫」→ 分阶段动画 → 铺满画布
2. **改图**：「给猫戴顶红色帽子」
3. **移动**：「移到右边」
4. **清空**：「清空画布」→ 立即清空
5. **复合**：「先清空画布，然后画一棵大树」
6. **导出**：「保存图片」或点「导出」
7. **防回声**：画完后 TTS 播报时不应再次触发绘图

---

## 9. 配置说明

`config.json` 关键字段：

| 字段 | 说明 |
|---|---|
| `llm_base` / `llm_key` / `llm_model` | DeepSeek 指令理解 |
| `image_api_key` | 阿里云百炼：ASR + 文生图 + 改图 |
| `image_model` | 默认 `wan2.6-t2i` |
| `image_edit_model` | 默认 `wan2.6-image` |
| `canvas_width` / `canvas_height` | 默认 960×540 |

# 语音绘图工具 · 设计文档

> **题目要求**：用户不使用鼠标或键盘操作画布，仅通过语音指令完成绘图创作。需综合考虑指令理解的准确性与容错性、语音到绘图操作的响应延迟，以及复杂指令的拆解与执行能力。

---

## 1. 问题定义

绘图是连续、精确的空间操作；语音是模糊、离散的自然语言。本项目的核心工程问题：

1. 如何把口语（含 ASR 识别错误）稳定映射为可执行的绘图指令？
2. 如何在可接受的延迟内完成「说完 → 出图」？
3. 如何把复合语句拆解为可顺序执行的操作序列？

**产品范式**：不做自由手绘（语音无法稳定表达连续轨迹坐标）。采用 **AI 文生图 + 对话式改图** 作为创作主路径，辅以 **参数化矢量图形** 处理简单几何与编辑操作。

---

## 2. 交付形态与技术选型

| 层次 | 选型 | 理由 |
|---|---|---|
| 应用形态 | Web 应用（`run_web.bat`） | 现代 UI、云端 ASR 快而准、Canvas 动画效果好 |
| 前端 | HTML + Canvas + ES Modules | 笔刷揭示动画、实时状态反馈 |
| 后端 | FastAPI + uvicorn | 统一代理 LLM / 万相 / ASR |
| ASR | 阿里云 Paraformer（主）+ Whisper（备） | 国内网络稳定、识别快；Whisper 作兜底 |
| NLU | DeepSeek LLM（主脑） | 理解任意自然语言，拆解复合指令 |
| 文生图 | 通义万相 `wan2.6-t2i` | 高质量插画/场景生成 |
| 改图 | 通义万相 `wan2.6-image` | 在原图基础上按指令局部编辑 |
| TTS | 浏览器 SpeechSynthesis | 语音反馈闭环 |

### 纯语音交互说明

- **画布操作**：全部通过语音指令驱动，用户无需用鼠标/键盘在画布上绘制或选中。
- **必要例外**：首次需点击「开始聆听」以获取浏览器麦克风权限（浏览器安全策略限制，无法纯语音授权）。
- 侧边栏提供文字输入作为**调试/无障碍备选**，正式演示流程可全程语音。

---

## 3. 系统架构

```
麦克风 (MediaRecorder)
      ↓ 客户端 VAD（自适应阈值，过滤环境杂音）
音频片段 → POST /api/transcribe
      ↓ 阿里云 Paraformer / Whisper 兜底
识别文本
      ↓ POST /api/chat（附带画布上下文 + 对话历史）
DeepSeek LLM → 结构化指令 JSON
      ↓
前端 Executor 串行执行
      ├─ generate_image → POST /api/generate-image → 通义万相文生图
      ├─ edit_image     → POST /api/edit-image     → 通义万相图像编辑
      ├─ stroke / place / move / resize / delete / undo … → Canvas 引擎
      └─ 笔刷揭示动画（revealImage）模拟「一笔笔画出来」
      ↓
TTS 语音反馈 + 对话日志
```

---

## 4. 三大要求的实现方案

### 4.1 指令理解准确性与容错性

| 机制 | 实现 |
|---|---|
| LLM 主脑 | 理解任意口语，输出结构化 `commands` 数组 |
| 画布上下文注入 | 将当前图形列表、AI 图片描述、颜色分组传给 LLM，支持「移动小猫」「给猫戴帽子」等指代 |
| 对话历史 | 最近 4 轮对话传入 LLM，支持多轮修正 |
| 指令规范化 | `_normalize_commands` 补全缺省坐标/尺寸，过滤无效笔画 |
| 空结果重试 | LLM 返回空 `commands` 时自动二次请求 |
| 网络容错 | 连接中断（10053）自动重试 + `Connection: close` |
| 破坏性确认 | 「清空画布」需语音说「确认/取消」 |
| 澄清反馈 | 无法理解时 TTS + 日志提示换说法，不静默失败 |

### 4.2 语音到绘图响应延迟

| 环节 | 典型延迟 | 优化手段 |
|---|---|---|
| VAD 分句 | ~0.6–1s 静音检测 | 自适应噪声阈值，减少误触发 |
| 云端 ASR | 1–3s | Paraformer 替代本地 Whisper（省去模型加载） |
| LLM 理解 | 3–15s | 45s 超时 + 连接重试；仅传必要上下文 |
| 文生图 | 10–30s | 异步 loading 提示；用户可继续等待 |
| 改图 | 15–40s | 同上 |
| 矢量指令 | <100ms | 本地 Canvas 即时渲染 |
| 笔刷揭示 | ~2.4s | `requestAnimationFrame` 动画，有「在画」的过程感 |

**瓶颈**：AI 生成/改图受云端 API 限制，无法做到「说完立刻出高质量图」。策略是：简单几何即时响应，复杂创作走 AI 并给出明确等待提示。

### 4.3 复杂指令拆解与执行

LLM 将复合语句拆解为 `commands` 数组，前端按类型分阶段串行执行：

| 阶段 | 指令类型 | 示例 |
|---|---|---|
| 1 | 非绘制类 | 清空、撤销、移动、删除、缩放 |
| 2 | 矢量笔画 | `stroke` 序列逐笔动画 |
| 3 | 文生图 | `generate_image` → 笔刷揭示 |
| 4 | 改图 | `edit_image` → 替换原图 → 重新揭示 |

**示例**：「先清空画布，然后画一只橘猫，再把背景换成星空」

```json
[
  {"action":"clear"},
  {"action":"generate_image","prompt":"一只可爱的橘色小猫…","x":480,"y":270,"w":480,"h":480},
  {"action":"edit_image","prompt":"把背景换成星空夜空"}
]
```

- **多轮对话改图**：画布保留上一张图的 `prompt` 描述，LLM 知道「猫」指哪张图。
- **颜色分组移动**：`move_group` 按颜色移动整组矢量图形。
- **指代**：「把它放大」→ `resize` 作用于最近选中/创建对象。

---

## 5. 指令能力：计划 vs 实现

| 能力 | 示例 | 计划 | 实现 | 说明 |
|---|---|:---:|:---:|---|
| **AI 文生图** | 画一只可爱的小猫 | ✅ | ✅ | 通义万相，默认创作方式 |
| **对话式改图** | 给猫戴顶帽子 / 背景换夜空 | ✅ | ✅ | `wan2.6-image` 在原图基础上编辑 |
| **笔刷揭示动画** | 图逐步显现 | ✅ | ✅ | 16 条横向扫描带 + 缓动 |
| 绘制基本图形 | 画一个红色的圆 | ✅ | ✅ | 圆/方/三角/线/星等 |
| 矢量笔画 | 画一条线 / 涂鸦 | ✅ | ✅ | `stroke` 逐笔动画（简单线条） |
| 颜色 / 位置 / 尺寸 | 蓝色、左上角、大一点 | ✅ | ✅ | 12 色 + 9 锚点 + 语义尺寸 |
| 批量 + 布局 | 三个方块排成一行 | ✅ | ✅ | 行/列布局 |
| 移动 / 缩放 / 旋转 / 改色 | 移到右边 / 放大 | ✅ | ✅ | 含 `move_group` 整组移动 |
| 删除 / 清空（需确认） | 删除 / 清空画布 | ✅ | ✅ | |
| 撤销 / 重做 | 撤销 | ✅ | ✅ | 历史栈最多 100 步 |
| 预设组合 | 画房子 / 太阳 / 树 | ✅ | ✅ | 多图元模板 |
| 文字 | 写上「你好」 | ✅ | ✅ | |
| 复合指令 | …然后…再… | ✅ | ✅ | LLM 拆解为多步 `commands` |
| 多轮对话 | 先画猫，再戴帽子 | ✅ | ✅ | 对话历史 + 画布上下文 |
| 云端语音识别 | 说完自动识别 | ✅ | ✅ | 阿里云 Paraformer |
| 本地 ASR 兜底 | Whisper 离线 | ✅ | ✅ | 云端不可用时降级 |
| 语音反馈 TTS | 播报执行结果 | ✅ | ✅ | 浏览器 SpeechSynthesis |
| 语音帮助 | 说「帮助」 | ✅ | ✅ | 弹出指令说明 |

---

## 6. 未完成 / 受限部分及原因

| 项目 | 状态 | 原因 |
|---|---|---|
| 完全零键鼠（含启动） | ⚠️ 受限 | 浏览器麦克风权限必须用一次点击授权；保留文字输入作备选 |
| 唤醒词免手启动 | ❌ 未实现 | 需常驻监听 + 唤醒词模型；当前用「开始聆听」按钮 |
| 自由手绘 / 任意笔迹 | ❌ 未实现 | 语音无法稳定表达连续坐标；已用 AI 文生图替代 |
| 矢量画复杂物体 | ❌ 放弃 | LLM 输出坐标质量差；已改为文生图 |
| 序数指代（「第二个圆」） | ⚠️ 部分 | 支持「最近创建/选中」和颜色分组；精确序数需扩展状态机 |
| 多图元素组合布局 | ⚠️ 部分 | 支持多次 `generate_image`；未做智能排版防重叠 |
| 局部蒙版精修 | ❌ 未实现 | 当前改图为全图指令编辑，非像素级蒙版 |
| 离线 AI 创作 | ❌ 未实现 | 文生图/改图/LLM 均依赖云端 API |
| 实时流式 ASR 字幕 | ❌ 未实现 | 当前为「说完 → 整句识别」，非边说边出字 |
| 图形分组 / 图层管理 | ❌ 未实现 | 语音操作成本高，优先级低 |

---

## 7. 模块说明

| 文件 / 目录 | 职责 |
|---|---|
| `run_web.bat` | 一键启动 Web 服务并打开浏览器 |
| `voice_paint/web_server.py` | FastAPI：ASR / LLM / 文生图 / 改图 API |
| `voice_paint/cloud_asr.py` | 阿里云 Paraformer 语音识别 |
| `voice_paint/whisper_service.py` | 本地 Whisper 加载与转写（兜底） |
| `voice_paint/image_gen.py` | 通义万相文生图 + 图像编辑 |
| `voice_paint/nlu/llm.py` | DeepSeek 主脑：理解、拆解、路由 |
| `web/js/app.js` | 前端主逻辑：语音管线、指令调度 |
| `web/js/recorder.js` | 麦克风录音 + VAD + 上传 ASR |
| `web/js/engine.js` | Canvas 引擎 + 笔刷揭示动画 |
| `web/js/executor.js` | 指令队列、确认、反馈 |
| `config.json` | API Key、模型、画布尺寸等配置 |

---

## 8. 测试建议

1. **文生图**：说「画一只橘猫」→ 等待约 15s → 猫以笔刷动画显现
2. **改图**：接着说「给猫戴顶红色帽子」→ 同一只猫戴上帽子
3. **改背景**：说「把背景换成星空」→ 背景变化、猫保留
4. **移动**：说「移到画布最右边」→ 图片右移
5. **复合**：说「先清空画布，然后画一棵大树」→ 清空确认后生成新图
6. **简单几何**：说「画一个红色的圆」→ 即时出现
7. **撤销**：说「撤销」→ 回到上一步
8. **容错**：小声说话或环境嘈杂 → 观察 VAD 是否误触发；网络断开 → 观察重试提示

---

## 9. 配置说明

`config.json` 关键字段：

| 字段 | 说明 |
|---|---|
| `llm_base` / `llm_key` / `llm_model` | DeepSeek（或兼容 API）用于指令理解 |
| `image_api_key` | 阿里云百炼 Key，用于 ASR + 文生图 + 改图 |
| `image_model` | 文生图模型，默认 `wan2.6-t2i` |
| `image_edit_model` | 改图模型，默认 `wan2.6-image` |
| `whisper_model` | 本地 ASR 兜底，默认 `tiny` |

---

## 10. 演进方向（未纳入本次交付）

1. 唤醒词（「小绘，画一只猫」）实现真正免手启动
2. 多元素智能排版（「左边猫右边狗」自动防重叠）
3. 局部蒙版编辑（「只改猫的眼睛」）
4. 指令测试集 + 自动评测（准确率 / P95 延迟指标）
5. 流式 ASR（边说边显示字幕）
